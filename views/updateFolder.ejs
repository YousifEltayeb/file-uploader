<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update folder title</title>
  </head>
  <body>
    <form action="/folders/update/<%= folder.id %>">
      <label for="title">New folder name: </label>
      <input type="text" name="title" value="<%= folder.title %>" />
      <button type="submit">Update folder</button>
    </form>
  </body>
</html>
